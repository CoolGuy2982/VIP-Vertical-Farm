# VIP Vertical Farm — Dependencies
# =================================

# Gemini API
google-genai>=1.55.0

# API server
fastapi>=0.115.0
uvicorn>=0.34.0
pydantic>=2.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0

# Camera (install on Pi and dev machines)
opencv-python-headless>=4.9.0
Pillow>=10.0

# Firebase cloud backup
firebase-admin>=6.0

# Jetson Nano only (installed by setup script on the device)
Jetson.GPIO>=2.0.0

# Wi-Fi smart plug control (water pump)
python-kasa>=0.7.0
